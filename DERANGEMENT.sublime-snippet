<snippet>
	<content><![CDATA[
int derangement(int n){
  vector<int> dp(n + 1);
  dp[2] = 1;
  for(int i = 3; i <= n; i++){
    dp[i] = (i - 1) * (dp[i - 1] + dp[i - 2]) % M;
  }
  return dp[n];
}

$1
]]></content>
	<tabTrigger>DERANGEMENT</tabTrigger>
	<scope>source.c++</scope>
</snippet>

