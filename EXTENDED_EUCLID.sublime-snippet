<snippet>
	<content><![CDATA[
pii e_gcd(int a, int b){
  if(!b)return {1, 0};
  auto [x1, y1] = e_gcd(b, a%b);
  int x = y1;
  int y = x1 - (a / b)*y1;
  return {x, y};
}
$1
]]></content>
	<tabTrigger>EXTENDED_EUCLID</tabTrigger>
	<scope>source.c++</scope>
</snippet>

